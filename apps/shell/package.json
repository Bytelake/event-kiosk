{
  "name": "shell",
  "version": "0.0.1",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "copy:assets": "cp src/registration-chrome.html src/keyboard.html dist/",
    "dev": "tsc && npm run copy:assets && electron .",
    "build": "tsc && npm run copy:assets",
    "start": "electron ."
  },
  "dependencies": {
    "electron": "^43.4.1"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5"
  }
}

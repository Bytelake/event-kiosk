{
  "name": "shell",
  "version": "0.0.1",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc && electron .",
    "build": "tsc && cp src/registration-chrome.html src/registration-keyboard.html dist/",
    "start": "electron ."
  },
  "dependencies": {
    "electron": "^43.4.1"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5"
  }
}

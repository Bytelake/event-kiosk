# Raspberry Pi OS Lite (deprecated path)

This directory is **deprecated**. Use [`deploy/debian/`](../debian/) instead — Raspberry Pi OS is a Debian-based target.

| Old path | Use instead |
|----------|-------------|
| `deploy/pi-os-lite/install.sh` | `deploy/debian/install.sh` |
| `event-kiosk-pi-*.tar.gz` | `event-kiosk-debian-arm64-*.tar.gz` (pi name kept as alias) |

Scripts here are thin wrappers for backward compatibility.
